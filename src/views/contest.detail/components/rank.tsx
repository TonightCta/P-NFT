import { ReactElement, ReactNode } from "react";

const RankCard = () : ReactElement<ReactNode> => {
    return (
        <div className="rank-card">
            
        </div>
    )
};

export default RankCard;