import { ReactElement,ReactNode } from "react";

const VisionMap = () : ReactElement<ReactNode> => {
    return(
        <div className="sivion-map public-screen">
            Vision Map
        </div>
    )
};

export default VisionMap;