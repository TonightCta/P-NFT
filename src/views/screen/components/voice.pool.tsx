import { ReactElement,ReactNode } from "react";

const VoicePool = () : ReactElement<ReactNode> => {
    return(
        <div className="voice-poll public-screen">
            Voice Pool
        </div>
    )
};

export default VoicePool;