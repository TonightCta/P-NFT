import { ReactElement,ReactNode } from "react";

const BuildIN = () : ReactElement<ReactNode> => {
    return(
        <div className="build-index public-screen">
            <h1>AI Building in PIZZAP</h1>
        </div>
    )
};

export default BuildIN;