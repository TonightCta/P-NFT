import { ReactElement,ReactNode } from "react";
import './index.scss';

const FooterWapper = () : ReactElement<ReactNode> => {
    return (
        <div className="footer-wapper"></div>
    )
};

export default FooterWapper;